'use client';

import { RequestCasePage, RequestCasePageProps } from '@aei/app/src/app/(public)/request/[authority]/RequestCasePage';

export default function Page(props: RequestCasePageProps) {
  return <RequestCasePage {...props} />;
}
