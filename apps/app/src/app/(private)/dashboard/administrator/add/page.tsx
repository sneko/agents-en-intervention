'use client';

import { AddAdminPage, AddAdminPageProps } from '@aei/app/src/app/(private)/dashboard/administrator/add/AddAdminPage';

export default function Page(props: AddAdminPageProps) {
  return <AddAdminPage {...props} />;
}
