'use client';

import { MetricsPage, MetricsPageProps } from '@aei/app/src/app/(private)/dashboard/metrics/MetricsPage';

export default function Page(props: MetricsPageProps) {
  return <MetricsPage {...props} />;
}
