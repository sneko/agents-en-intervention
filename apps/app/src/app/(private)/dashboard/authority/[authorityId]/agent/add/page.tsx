'use client';

import { AddAgentPage, AddAgentPageProps } from '@aei/app/src/app/(private)/dashboard/authority/[authorityId]/agent/add/AddAgentPage';

export default function Page(props: AddAgentPageProps) {
  return <AddAgentPage {...props} />;
}
