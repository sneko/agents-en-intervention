'use client';

import { CasePage, CasePageProps } from '@aei/app/src/app/(private)/dashboard/authority/[authorityId]/case/[caseId]/CasePage';

export default function Page(props: CasePageProps) {
  return <CasePage {...props} />;
}
